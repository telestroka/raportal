<ul id="menu">	<?php		foreach ($site->CONFIG['menu'] as $menu_page => $menu_title)		{			$url = ($menu_page == '_index') ? '/' : $site->URL . '/' . str_replace('_','/',$menu_page) . '.php';			if ($menu_page == $module->NAME . '_' . $page->NAME) echo '<li class="active">' . $menu_title . '</li>';			else echo '<li><a href="' . $url . '">' . $menu_title . '</a></li>';		}	?></ul>